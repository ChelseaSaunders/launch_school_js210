var foo; // creates variable foo, set to undefined
var bar = "qux"; // creates variable bar, set to string "qux"

let foo; // creates variable foo, set to undefined
let bar = "qux"; // creates variable bar, set to string "qux"
const baz = 3.1415 // creates variable baz set to 3.1415 that cannot be changed

// Var has no const equivilent
