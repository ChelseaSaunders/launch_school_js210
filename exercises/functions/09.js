/* What will the following code log to the console and why? Don't run the code 
  until after you have tried to answer. */

console.log(a); // undefined...with hoisting var a gets moved to the top but the
// value assignment doesn't so it resolves to undefined

var a = 1;